<ul>
			<li>
           <a href="<%= user_path(current_user) %>"> <%= pluralize(@user.microposts.count, "Beer Post") %> </a>
            </li>
        <li>
        <% @user ||= current_user %>
        <a href="<%= following_user_path(@user) %>">
            <%= @user.following.count %> following
        </a>
        </li>
        <li>
        <a href="<%= followers_user_path(@user) %>">
            <%= pluralize(@user.followers.count, "follower") %></a>
        </li>
        <li>
        	<%= @user.microposts.count + @user.following.count +  @user.followers.count %> Total Beer Badges
        	</li>
        </ul>